#include "main.h"
#include <string.h>

/**
 * _strlen - function returns length of string
 * @s: pointer of s
 * Return: 0
 */

int strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
